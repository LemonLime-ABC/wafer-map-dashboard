# 웨이퍼 맵 불량 패턴 분류 및 수율 분석 시스템 — 실행 계획

**데이터셋**: WM-811K (Wafer Map Defect Pattern)
**프레임워크**: PyTorch
**선행 프로젝트**: SECOM 공정 센서 기반 수율 이상 진단 시스템

---

## 1. 프로젝트 목표

세 가지를 순서대로 달성한다.

1. **시각화** — 웨이퍼 맵 불량 패턴을 이미지로 확인하고 데이터 구조를 파악
2. **예측 모델** — CNN으로 9개 불량 패턴 분류, 클래스별 성능 정량화
3. **수율 분석** — 패턴을 공정 모듈에 매핑하고 Lot 단위 관리도로 이상 감지

### 1.1 SECOM 프로젝트와의 관계

| 항목 | SECOM | WM-811K |
|---|---|---|
| 데이터 | 센서 590개 (익명화) | 웨이퍼 맵 이미지 |
| 관측 시점 | 공정 진행 중 | 검사 완료 후 |
| 원인 규명 수준 | "센서 59" (공정 미상) | "Edge-Ring → RTP 온도 이상" |
| 예측 성능 | PR-AUC 0.199 (데이터 한계) | 90% 이상 달성 가능 |
| 모델 | XGBoost | CNN |
| 해석 도구 | TreeSHAP | Grad-CAM |

SECOM에서 확보하지 못한 두 가지 — **실용 수준의 예측 성능**과 **원인의 공정 연결** — 를 이 프로젝트에서 채운다.

### 1.2 SECOM에서 계승하는 방법론

| 원칙 | SECOM에서의 적용 | WM-811K에서의 적용 |
|---|---|---|
| 정확도 불신 | 불량 6.6% → 정확도 93.4% 함정 | None 85% → 정확도 무의미. macro-F1 사용 |
| 데이터 누수 차단 | 전처리를 fold 내부에서만 수행 | **증강을 학습 fold 내부에서만 수행** |
| 정직한 평가 | OOF(out-of-fold) 예측 사용 | 교차검증 예측 사용 |
| 한계 명시 | 센서 130 감지 실패를 결과로 보고 | Near-full 성능 부족을 결과로 보고 |
| 배치 단위 확장 | 배치 100장 p-관리도 | Lot 단위 패턴 빈도 관리도 |

---

## 2. 데이터셋 실태

### 2.1 규모

| 구분 | 수량 |
|---|---|
| 전체 웨이퍼 맵 | 811,457장 |
| 라벨된 맵 | 172,950장 |
| 미라벨 | 638,570장 |
| 데이터 크기 | 약 3.5GB |
| 출처 Lot 수 | 46,293개 |

각 픽셀은 세 값 중 하나를 갖는다: `0` = 다이 없음(배경), `1` = 정상 다이, `2` = 불량 다이.

### 2.2 클래스 분포 (라벨된 172,950장 기준)

| 클래스 | 장수 | 비율 | 증강 후(8배) | 학습 가능성 |
|---|---|---|---|---|
| None | 147,431 | 85.2% | — (축소 대상) | 충분 |
| Edge-Ring | 9,680 | 5.6% | — (축소 대상) | 충분 |
| Edge-Loc | 5,189 | 3.0% | — (축소 대상) | 충분 |
| Center | 4,294 | 2.5% | — (축소 대상) | 충분 |
| Loc | 3,593 | 2.1% | — (축소 대상) | 충분 |
| Scratch | 1,193 | 0.7% | 9,544 | 가능 |
| Random | 866 | 0.5% | 6,928 | 가능 |
| Donut | 555 | 0.3% | 4,440 | 주의 필요 |
| **Near-full** | **149** | **0.09%** | 1,192 | **불안정 예상** |

**핵심 제약**: 총량은 17만 장이지만 Near-full은 149장뿐이다. 5-Fold 교차검증 시 fold당 검증 샘플이 약 30장으로, 3장 차이만으로 Recall이 10% 요동친다. SECOM에서 불량 104장을 21장씩 쪼갰을 때 fold별 PR-AUC가 0.158~0.261로 널뛴 것과 같은 구조다.

일부 선행 연구는 이 문제로 Near-full과 None을 실험에서 제외했다. 본 프로젝트는 우선 포함하여 진행하고, 성능이 확보되지 않으면 그 사실 자체를 결과로 보고한다.

---

## 3. 단계별 실행 계획

### Phase 1 — 데이터 확보 및 실태 파악 (2~3일)

**목적**: 계획의 전제를 실제 데이터로 검증한다. 아래 숫자들은 문헌 기준이므로 직접 확인이 필요하다.

**작업**

1. `WM811K.pkl` 다운로드 및 로드
2. 클래스별 실제 장수 집계 — 2.2절 표와 대조
3. **웨이퍼 맵 크기 분포 확인** ← 최대 난관
4. Lot 정보 존재 여부 및 형태 확인 (Phase 6의 시계열 분석 가능 여부를 좌우)
5. 픽셀 값 분포 확인 (0/1/2 외의 값이 있는지)

**크기 불일치 문제**

웨이퍼 맵은 크기가 제각각이다(26×26부터 300×200 이상까지). 두 가지 선택지가 있으며 Phase 1 결과를 보고 결정한다.

| 방안 | 장점 | 단점 |
|---|---|---|
| 전체를 64×64로 리사이즈 | 데이터 전량 활용 | 작은 맵이 뭉개져 패턴 손실 |
| 특정 크기 그룹만 선별 | 정보 손실 없음 | 데이터 감소 |

**판단 기준**: 상위 몇 개 크기가 전체의 몇 %를 차지하는지. 특정 크기가 지배적이면 선별이 유리하다.

**산출물**
- `01_data_survey.ipynb` — 실태 조사 노트북
- 클래스·크기 분포 요약표

---

### Phase 2 — 시각화 (2~3일)

**목적**: 프로젝트 목표 1번. 데이터를 눈으로 이해하고 발표 자료의 기반을 만든다.

**작업**

1. **패턴 그리드** — 9개 클래스 × 각 6장, 64×64로 통일해 출력
2. 크기 분포 히스토그램
3. 클래스 불균형 시각화 (로그 스케일 막대)
4. 클래스별 평균 맵 — 같은 클래스 이미지를 평균내면 패턴의 전형이 드러난다
5. Lot 단위 시계열 (Phase 1에서 Lot 정보 확인된 경우)

**여기서 확인해야 할 것**

Edge-Loc과 Scratch가 실제로 얼마나 유사한지 눈으로 확인한다. 두 패턴은 모두 웨이퍼 가장자리 근처의 길쭉한 클러스터 형태를 보이지만 근본 원인이 전혀 다르다(열공정 vs 기계적 취급). 이 유사성이 Phase 4의 혼동 행렬에서 핵심 논점이 된다.

**산출물**
- `02_visualization.ipynb`
- 패턴 그리드 이미지 (발표 자료용)

---

### Phase 3 — 데이터 준비 (3~4일)

**목적**: 학습 가능한 형태로 데이터를 구성한다.

#### 3.1 샘플링 전략

```
None:      2,000장   (147,431에서 축소)
Edge-Ring: 2,000장   (9,680에서 축소)
Edge-Loc:  2,000장   (5,189에서 축소)
Center:    2,000장   (4,294에서 축소)
Loc:       2,000장   (3,593에서 축소)
Scratch:   1,193장   (전량 사용)
Random:      866장   (전량 사용)
Donut:       555장   (전량 사용)
Near-full:   149장   (전량 사용)
───────────────────────────────
합계 약 12,763장
```

다수 클래스만 축소하고 소수 클래스는 전량 사용한다. 원본의 85:0.09 비율을 약 13:1 수준으로 완화한다.

#### 3.2 데이터 증강

웨이퍼 맵은 **회전과 뒤집기가 라벨을 바꾸지 않는다**. Center 패턴을 90도 회전해도 여전히 Center이며, Edge-Ring도 마찬가지다. 일반 사진과 달리 이 변환이 물리적으로 타당하다.

```
회전 4방향 (0°, 90°, 180°, 270°) × 좌우 뒤집기 2 = 8배
```

**적용 대상**: Donut, Near-full, Random, Scratch (소수 클래스)

**필수 제약**: 증강은 **학습 fold 내부에서만** 수행한다. 분할 이전에 증강하면 같은 원본에서 파생된 변형이 학습셋과 검증셋에 나뉘어 들어가 데이터 누수가 발생한다. SECOM에서 SMOTE를 분할 전에 적용하면 안 되는 것과 정확히 같은 논리다.

```python
# 올바른 순서
for train_idx, val_idx in skf.split(X, y):
    X_tr = augment(X[train_idx])   # 학습 fold만 증강
    X_val = X[val_idx]             # 검증 fold는 원본 그대로
```

#### 3.3 Near-full 처리 방안

| 방안 | 내용 | 채택 조건 |
|---|---|---|
| A | 149장 + 증강으로 포함 | **기본 채택.** 결과를 보고 판단 |
| B | 8클래스로 축소 (Near-full 제외) | A가 실패하고, 제외가 정당화될 때 |
| C | 규칙 기반 별도 탐지 | Near-full은 "불량 다이 비율이 극히 높음"이라는 단순 특성이 있어 임계값으로 탐지 가능 |

방안 A로 시작한다. 성능이 확보되지 않으면 그 사실 자체를 분석 결과로 보고하며, 이는 SECOM에서 센서 130의 감지 실패를 정직하게 표시한 것과 같은 처리다.

**산출물**
- `03_preprocessing.py`
- 전처리된 학습 데이터 (`.npz` 또는 `.pt`)

---

### Phase 4 — CNN 모델 학습 (1주)

**목적**: 프로젝트 목표 2번.

#### 4.1 모델 구조

작은 모델로 시작한다. 1만 장 규모에 ResNet 같은 대형 모델은 과하다.

```
입력 (1채널, 64×64)
  ↓
Conv(32, 3×3) → BatchNorm → ReLU → MaxPool
Conv(64, 3×3) → BatchNorm → ReLU → MaxPool
Conv(128, 3×3) → BatchNorm → ReLU → MaxPool
  ↓
GlobalAveragePooling
Dropout(0.3)
Linear(9)
```

**입력 표현 검토 사항**: 픽셀 값 0/1/2를 그대로 쓸지, 3채널 원-핫으로 분리할지. 후자가 "배경"과 "정상 다이"와 "불량 다이"를 명확히 구분하지만 입력 크기가 3배가 된다. Phase 4 초반에 두 방식을 비교한다.

#### 4.2 학습 설정

| 항목 | 설정 | 근거 |
|---|---|---|
| 손실 함수 | CrossEntropyLoss (class weight 적용) | 클래스 불균형 보정 |
| 최적화 | Adam, lr=1e-3 | 표준 설정으로 시작 |
| 배치 크기 | 64 | GPU 메모리 여유 |
| 에폭 | 30~50 (early stopping) | 과적합 방지 |
| 검증 | 5-Fold Stratified CV | SECOM과 동일 |

#### 4.3 평가 지표

**주 지표**: macro-F1, balanced accuracy

두 지표 모두 클래스를 동등하게 취급하므로 소수 클래스 성능에 민감하다. None이 85%를 차지하는 데이터에서 일반 정확도는 모델 성능을 판별할 수 없다.

**보조 지표**: 클래스별 Precision/Recall, 9×9 혼동 행렬

**필수 산출물 — 혼동 행렬**

Edge-Loc과 Scratch가 서로 얼마나 혼동되는지가 이 프로젝트의 핵심 발견이 될 가능성이 높다. 두 패턴은 형태가 유사하나 근본 원인이 열공정과 기계적 취급으로 전혀 달라, 오분류 시 팹의 시정 조치가 엉뚱한 모듈로 향하게 된다.

**산출물**
- `04_train.py`, `04_evaluate.ipynb`
- 학습된 모델 가중치
- 혼동 행렬, 클래스별 성능표

---

### Phase 5 — 원인 분석 (4~5일)

**목적**: 예측 근거를 확인하고 공정 지식과 연결한다.

#### 5.1 Grad-CAM

CNN이 이미지의 어느 영역을 근거로 판단했는지 히트맵으로 시각화한다. SECOM의 SHAP에 대응하는 이미지 해석 도구다.

**검증 방법**: 예측 클래스와 주목 영역의 일치 여부를 확인한다.

| 예측 | 기대되는 주목 영역 | 어긋나면 |
|---|---|---|
| Edge-Ring | 가장자리 전체 링 | 우연히 맞힌 것 |
| Center | 중앙 영역 | 근거 부적절 |
| Scratch | 선형 클러스터 | 형태 인식 실패 |

**정량화**: 예측 클래스별로 Grad-CAM 주목 영역이 해당 패턴의 기대 위치와 일치하는 비율을 산출한다. SECOM에서 "SHAP 원인 지목 정확도 87~90%"를 계산한 것과 같은 방식이다.

#### 5.2 공정 원인 매핑

| 패턴 | 공정 모듈 | 물리적 원인 |
|---|---|---|
| Center | 챔버 | RF 동작 불규칙, 유체 흐름 불균일 |
| Donut | 세정 | 포토레지스트 잔류물 미제거 |
| Edge-Ring | RTP | 온도 조절 이상, 저온 공정 편차 |
| Edge-Loc | 어닐링 / 로드락 | 온도 불균일, 밸브 오염 |
| Loc | 국소 설비 | 특정 위치 반복 결함 |
| Scratch | 핸들링 | 기계적 접촉, 이송 장비 |
| Random | 파티클 | 오염, 환경 요인 |
| Near-full | 전면 이상 | 공정 전체 실패 |

**Edge-Loc vs Scratch 문제**

두 패턴은 웨이퍼 가장자리 근처의 길쭉한 클러스터라는 점에서 겉보기가 유사하다. 그러나 조치가 완전히 다르다.

- Edge-Loc → **열공정 튜닝** (온도 프로파일 조정)
- Scratch → **기계적 취급 점검** (이송 장비 검사)

오분류하면 시정 조치가 엉뚱한 공정 모듈로 향한다. 혼동 행렬의 이 칸이 실무적으로 가장 중요하며, 혼동률을 정량화하는 것 자체가 분석 결과가 된다.

**산출물**
- `05_gradcam.ipynb`
- 패턴별 Grad-CAM 예시 이미지
- 공정 매핑 테이블

---

### Phase 6 — 수율 분석 (4~5일)

**목적**: 프로젝트 목표 3번. SECOM의 관리도 로직을 재사용한다.

#### 6.1 Lot 단위 관리도

SECOM에서 구축한 p-관리도 로직을 그대로 적용하되 대상만 바꾼다.

| | SECOM | WM-811K |
|---|---|---|
| 집계 단위 | 배치 100장 | Lot |
| 관측값 | 위험 판정 개수 | 패턴별 발생 빈도 |
| 중심선 | 정상 배치 평균 | 전체 기간 패턴 발생률 |
| 관리한계 | 평균 + 3σ | p̄ + 3√(p̄(1−p̄)/n) |
| 판정 | Z값, 이항검정 p값 | 동일 |

**시나리오 예시**

```
이번 주 Edge-Ring 발생률 12% (평소 5%, UCL 9.8%)
→ 관리한계 초과, p = 0.003
→ RTP 장비 온도 프로파일 점검 필요
```

#### 6.2 수율 개선 분석

패턴별 발생률과 공정 매핑을 결합해 개선 우선순위를 산출한다.

```
수율 손실 기여도 = 패턴 발생률 × 해당 패턴의 평균 불량 다이 비율
```

이를 통해 "어느 공정 모듈이 수율 손실에 가장 크게 기여하는가"를 정량화한다.

**산출물**
- `06_yield_analysis.ipynb`
- Lot 관리도, 패턴별 기여도 분석

---

### Phase 7 — 대시보드 (1주)

**목적**: SECOM 대시보드 구조를 재사용해 결과를 통합한다.

| 화면 | 내용 |
|---|---|
| 1. 웨이퍼 맵 진단 | 맵 이미지 + 예측 패턴 + Grad-CAM 히트맵 |
| 2. 패턴별 성능 | 9×9 혼동 행렬 + 클래스별 지표 + 혼동 쌍 분석 |
| 3. Lot 수율 모니터링 | p-관리도 + 이탈 구간 탐지 + Z값·p값 |
| 4. 공정 원인 매핑 | 패턴 → 모듈 → 조치 + 수율 기여도 |
| 5. 모델 방법론 | 검증 방식, 증강 누수 차단, 한계 명시 |

**기술 스택**: Streamlit + Plotly (SECOM과 동일)

**배포**: GitHub → Streamlit Community Cloud

**주의**: CNN 모델 파일이 XGBoost보다 크다. 무료 플랜의 메모리 제한을 고려해 경량 모델을 유지하고, 필요 시 예측 결과를 사전 계산해 저장한다(SECOM의 `precomputed.joblib` 방식).

---

## 4. 기술 스택

| 구분 | 도구 | 비고 |
|---|---|---|
| 딥러닝 | **PyTorch** | 신규 학습 |
| 해석 | Grad-CAM | 직접 구현 또는 `pytorch-grad-cam` |
| 데이터 | numpy, pandas | SECOM과 동일 |
| 평가 | scikit-learn | macro-F1, 혼동 행렬 |
| 시각화 | matplotlib, Plotly | |
| 대시보드 | Streamlit | SECOM과 동일 |
| 학습 환경 | Google Colab (무료 GPU) | 1~2만 장 규모에 충분 |

**SECOM에서 제외되는 것**: XGBoost, TreeSHAP

---

## 5. 예상 난관과 대응

| # | 난관 | 대응 |
|---|---|---|
| 1 | 웨이퍼 맵 크기 불일치 | Phase 1에서 분포 확인 후 리사이즈/선별 결정 |
| 2 | Near-full 149장 | 증강 적용, 실패 시 결과로 정직하게 보고 |
| 3 | Edge-Loc / Scratch 혼동 | 혼동률 정량화 자체를 분석 결과로 활용 |
| 4 | 증강 누수 | 학습 fold 내부에서만 증강 (코드 구조로 강제) |
| 5 | Colab 세션 종료 | 학습 완료 즉시 모델 저장, 드라이브 연동 |
| 6 | 모델 파일 크기 | 경량 CNN 유지, 예측 결과 사전 계산 |

---

## 6. 일정

| Phase | 내용 | 기간 |
|---|---|---|
| 1 | 데이터 확보·실태 파악 | 2~3일 |
| 2 | 시각화 | 2~3일 |
| 3 | 데이터 준비 | 3~4일 |
| 4 | CNN 학습 | 1주 |
| 5 | 원인 분석 | 4~5일 |
| 6 | 수율 분석 | 4~5일 |
| 7 | 대시보드 | 1주 |
| | **합계** | **약 5~6주** |

---

## 7. 성공 기준

**최소 달성 목표**

- 9개 클래스 중 7개 이상에서 F1 0.80 이상
- Grad-CAM 주목 영역이 패턴 위치와 일치함을 정량 확인
- Lot 단위 관리도로 이상 구간 탐지 시연
- 증강 누수 차단이 코드 수준에서 보장됨

**우수 달성 목표**

- macro-F1 0.85 이상
- Edge-Loc/Scratch 혼동률 10% 미만
- Near-full 포함 9개 클래스 전부에서 F1 0.70 이상
- 대시보드 배포 완료

**정직성 기준 (SECOM에서 계승)**

- 정확도 대신 macro-F1을 주 지표로 보고
- 클래스별 성능 편차를 숨기지 않고 명시
- 실패한 클래스가 있다면 그 원인과 함께 보고
- 데이터 증강 방식과 누수 차단 절차를 명시

---

## 8. 파일 구조 (예정)

```
wafer_map_project/
├─ PLAN.md
├─ data/
│   └─ WM811K.pkl
├─ notebooks/
│   ├─ 01_data_survey.ipynb
│   ├─ 02_visualization.ipynb
│   ├─ 04_evaluate.ipynb
│   ├─ 05_gradcam.ipynb
│   └─ 06_yield_analysis.ipynb
├─ src/
│   ├─ 03_preprocessing.py
│   ├─ 04_train.py
│   ├─ model.py
│   ├─ augment.py
│   └─ gradcam.py
├─ artifacts/
│   ├─ model.pt
│   ├─ predictions.npz
│   └─ confusion_matrix.npz
├─ app.py
└─ requirements.txt
```

---

## 9. 다음 작업

**Phase 1 착수** — 데이터 다운로드 및 실태 파악

본 계획서의 클래스 분포 수치는 문헌 기준이므로, 실제 데이터로 검증한 뒤 Phase 3의 샘플링 전략을 확정한다. 특히 웨이퍼 맵 크기 분포는 문헌에 명확히 정리된 자료가 없어 직접 확인이 필요하다.

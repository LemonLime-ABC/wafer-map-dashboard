# Claude Code에서 시작하기

이 폴더의 문서 두 개를 Claude Code가 읽고 프로젝트를 이어받는다.

---

## 1. 폴더 준비

새 프로젝트 폴더를 만들고 두 파일을 넣는다.

```
wafer_map_project/
├─ CLAUDE.md      ← 인수인계 문서 (자동으로 읽힘)
└─ PLAN.md        ← 실행 계획
```

`CLAUDE.md`는 Claude Code가 **자동으로 읽는 파일명**이다. 이름을 바꾸지 말 것.

---

## 2. Claude Code 실행

터미널에서 프로젝트 폴더로 이동한 뒤 실행한다.

```bash
cd wafer_map_project
claude
```

---

## 3. 첫 메시지

아래를 그대로 입력하면 된다.

```
CLAUDE.md와 PLAN.md를 읽고 Phase 1부터 시작해줘.

먼저 WM-811K 데이터를 받는 방법부터 안내하고,
받은 뒤에 클래스별 장수와 웨이퍼 맵 크기 분포를 확인하는
코드를 만들어줘.

나는 Python 초보이고 PyTorch는 처음이니,
코드에 한국어 주석을 달고 새 문법은 따로 설명해줘.
```

---

## 4. 진행 중 확인할 것

Claude Code가 다음을 지키는지 본인이 확인한다. 지키지 않으면 지적할 것.

| 항목 | 확인 내용 |
|---|---|
| 주석 | 코드에 한국어 주석이 있는가 |
| 문법 설명 | 처음 보는 문법을 설명했는가 |
| 완결성 | 조각이 아니라 실행 가능한 파일을 줬는가 |
| 증강 누수 | 증강을 **학습 fold 안에서만** 하는가 |
| 평가 지표 | 정확도가 아니라 macro-F1을 쓰는가 |
| 정직성 | 성능이 나쁠 때 그대로 보고하는가 |

특히 **증강 누수**는 이 프로젝트에서 가장 틀리기 쉬운 부분이다.
`augment()`가 `skf.split()` 바깥에 있으면 잘못된 것이다.

---

## 5. 막혔을 때

Claude Code가 맥락을 잃은 것 같으면 이렇게 말한다.

```
CLAUDE.md를 다시 읽고, 지금 우리가 어느 Phase에 있는지 확인해줘.
```

새 대화를 시작할 때도 마찬가지다. `CLAUDE.md`가 폴더에 있으면
Claude Code가 자동으로 읽으므로 맥락이 복원된다.

---

## 6. 진행 상황 기록

Phase가 끝날 때마다 `CLAUDE.md`의 9절(현재 상태와 다음 작업)을 갱신하면
다음 세션에서 이어가기 쉽다. Claude Code에게 갱신을 요청해도 된다.

```
Phase 1이 끝났으니 CLAUDE.md의 9절을 갱신해줘.
확인된 크기 분포와 클래스 장수도 4절에 반영해줘.
```

---

## 참고

SECOM 프로젝트 코드(`main2.py`, `app.py`)를 같은 폴더나 인접 폴더에 두면,
Claude Code가 직접 읽어 SPC 관리도 로직 등을 재사용할 수 있다.

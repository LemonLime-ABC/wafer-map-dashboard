# CLAUDE.md — 웨이퍼 맵 불량 패턴 분류 프로젝트

이 문서는 Claude Code가 이 프로젝트를 이어받기 위한 인수인계 문서다.
작업을 시작하기 전에 이 문서와 `PLAN.md`를 모두 읽을 것.

---

## 1. 작업자 정보 — 반드시 지킬 것

### 1.1 Python 숙련도

**작업자는 Python 초보자다.** 선행 프로젝트(SECOM)를 통해 pandas, numpy,
scikit-learn, XGBoost의 기본 사용법을 익혔으나, 문법 자체가 아직 익숙하지 않다.
PyTorch는 이번이 처음이다.

따라서 다음을 지킨다.

- **모든 코드에 한국어 주석**을 단다. 무엇을 하는지가 아니라 **왜 그렇게 하는지**를 적는다.
- **처음 나오는 문법은 별도로 설명**한다. 예: 데코레이터, 컨텍스트 매니저,
  리스트 컴프리헨션, PyTorch의 `nn.Module` 상속 구조 등.
- 코드를 제공한 뒤 **무엇을 확인해야 하는지** 명시한다.
- 조각 코드를 여러 개 던지지 말고 **실행 가능한 완결된 파일**로 제공한다.

### 1.2 작업 진행 방식

작업자는 결과를 이해하지 못한 채 넘어가는 것을 싫어한다. 명시적으로 요청한 바 있다:

> "내가 이번 프로젝트에 대해 완벽히, 정말 완벽히 이해할 수 있도록 1부터 100까지 지도해줘"

따라서 각 단계가 끝날 때마다:
- 결과 수치가 **무엇을 의미하는지** 해석해 준다
- 예상과 다르면 **왜 그런지** 원인을 짚는다
- 다음 단계로 넘어가기 전에 **이해했는지 확인**한다

### 1.3 진로 목표 (설계 판단의 기준)

- 1지망: **공정설계 PI(Process Integration)** — SK하이닉스, 삼성전자
- 2지망: 평가및분석(Data Science)

**PI 직무의 핵심 문제**는 "각 단위공정은 스펙을 만족하는데 통합하면 불량"이다.
이 프로젝트는 그 문제 구조와 연결되는 지점을 만들어야 한다.
구체적으로 **불량 패턴 → 공정 모듈 → 물리적 원인** 연결이 그 역할을 한다.

기술적 판단이 갈릴 때는 **"PI 면접에서 말할 수 있는가"**를 기준으로 삼는다.

---

## 2. 선행 프로젝트 (SECOM) — 무엇을 배웠고 무엇이 부족했나

### 2.1 결과 요약

UCI SECOM 데이터(웨이퍼 1,567장 × 센서 590개, 불량률 6.6%)로
공정 이상 진단 대시보드를 구축했다.

| 지표 | 값 |
|---|---|
| 5-Fold PR-AUC | 0.199 ± 0.049 (무작위 기준선 0.066의 약 3배) |
| Recall @0.20 | 52.9% (104장 중 55장) |
| Precision @0.20 | 15.6% (353장 지목 중 55장) |
| 주간 불량률-모델확률 상관 | **r = +0.908** |

### 2.2 확립된 원칙 — 이 프로젝트에서도 그대로 적용

이 원칙들은 여러 차례의 오류 발견과 수정을 거쳐 확립된 것이다.
**타협하지 않는다.**

**① 정확도를 신뢰하지 않는다**
SECOM에서 초기 모델이 정확도 92.7%를 기록하면서 불량 21장 중 0장을 검출했다.
불균형 데이터에서 정확도는 성능을 판별할 수 없다.
→ WM-811K에서는 None이 85%이므로 **macro-F1과 balanced accuracy**를 주 지표로 쓴다.

**② 데이터 누수를 코드 구조로 차단한다**
- 전처리 기준은 학습 fold 내부에서만 산출한다
- 임계값은 검증셋 성능이 아닌 out-of-fold 예측으로 결정한다
- SMOTE를 분할 전에 적용하지 않는다 (성능이 인위적으로 부풀려짐)

→ WM-811K에서는 **데이터 증강이 같은 위험을 갖는다.**
   회전·뒤집기 증강을 분할 전에 하면 같은 원본의 변형이 학습셋과 검증셋에
   나뉘어 들어간다. **반드시 학습 fold 내부에서만 증강한다.**

**③ 학습 데이터 예측을 성능으로 보고하지 않는다**
SECOM 대시보드가 한때 최종 모델로 학습 데이터를 재예측해
PR-AUC 0.999, Recall 100%를 표시하고 있었다. 이는 외운 답을 확인한 것이다.
OOF 기준으로는 0.170, 52.9%였다.
→ 모든 성능 수치는 **교차검증 예측 기준**으로 보고한다.

**④ 한계를 먼저 명시한다**
SECOM에서 센서 130은 +3σ 이탈을 주입해도 감지되지 않았다(Z=+0.61, p=0.35).
이를 숨기지 않고 "센서별 검출 민감도 차이"라는 분석 결과로 제시했다.
→ WM-811K에서 **Near-full 클래스가 같은 역할**을 할 가능성이 높다.

**⑤ 측정 공간을 확인한다**
SECOM에서 센서 조합의 교호작용을 확률 공간에서 재어 "+19% 시너지"라고
결론냈으나, 모델의 원 출력인 로그오즈 공간에서 재측정하니 **-1.4%로 사실상 0**이었다.
시그모이드의 볼록성이 만든 수학적 착시였다.
→ 지표를 계산하기 전에 **어느 공간에서 재는 것이 맞는지** 확인한다.

**⑥ 통제군을 둔다**
"센서를 흔들었더니 검출이 늘었다"만으로는 "아무거나 흔들어도 오른다"는
반박을 막을 수 없다. 무작위 센서 대조군을 두어 배제했다.
→ 성능 주장에는 항상 **비교 기준선**을 함께 제시한다.

### 2.3 SECOM의 한계 — 이 프로젝트가 채워야 할 것

**한계 1: 예측 성능**
여러 방법(대치 방식, 특성 선별, 규제, 모델 교체)을 시도했으나 모두
PR-AUC 0.19~0.21에 수렴했다. 데이터 자체의 신호가 약했다.
Random Forest 0.201, Logistic Regression 0.149로 모델 교체도 무의미했다.

**한계 2: 원인의 공정 미연결 (더 중요)**
SECOM 센서는 익명화되어 있어 "센서 59가 원인"까지만 말할 수 있고,
그것이 어느 공정 단계인지 알 수 없었다. 실제 excursion 분석에서
센서 300, 299, 165, 164 등이 이탈한 것을 찾았지만, 인접 번호가 함께 움직였다는
관찰 이상으로 나아갈 수 없었다.

**→ WM-811K는 두 한계를 모두 해결한다.**
- 성능: 웨이퍼 맵은 패턴이 기하학적으로 뚜렷해 90% 이상 달성 가능
- 원인: 각 패턴이 특정 공정 모듈과 문헌상 연결되어 있음

---

## 3. 이 프로젝트의 목표

작업자가 직접 정의한 세 가지:

1. **웨이퍼 맵 시각화** — 불량 패턴을 이미지로 확인
2. **분석 및 예측 모델 제작** — CNN으로 9개 패턴 분류
3. **공정에 대입해 수율 분석 및 개선** — 패턴을 공정 모듈에 매핑하고
   Lot 단위 관리도로 이상 감지

세부 실행 계획은 `PLAN.md` 참조.

---

## 4. 데이터셋 핵심 사실 (Phase 1에서 실측 완료 — 2026-08-16)

### 4.1 규모

| 구분 | 수량 |
|---|---|
| 전체 | 811,457장 |
| 라벨됨 | 172,950장 |
| 미라벨 | 638,507장 (문헌상 638,570은 오기 — 811,457−172,950=638,507이 정확) |
| 파일 크기 | 실제 pkl 214MB(zip)/2,095.5MB(압축 해제, LSWMD.pkl) — 문헌상 "3.5GB"와 다름 |
| Lot 수 | 46,293개 (실측 정확히 일치) |

픽셀 값: `0` = 다이 없음(배경), `1` = 정상 다이, `2` = 불량 다이 (실측: 세 값 외 이상값 없음 확인)

**파일명 주의**: 실제 Kaggle 배포 파일명은 `WM811K.pkl`이 아니라 `LSWMD.pkl`이다.
**컬럼명 주의**: 원본 데이터셋 자체에 오타가 있다 — `trainTestLabel`이 아니라 `trianTestLabel`로 저장되어 있다. 코드에서 이 철자를 그대로 써야 한다.
**로드 시 호환성 이슈**: `LSWMD.pkl`은 2018년경 Python2 + pandas 0.2x대로 만들어져, 최신 pandas로 바로 `pd.read_pickle()`이 안 된다(옛 `pandas.indexes` 모듈 경로 + Python2 문자열 인코딩 문제). 해결한 로더가 `src/data_loader.py`의 `load_wm811k()`이며, 이후 모든 Phase는 이 함수를 재사용한다.

### 4.2 클래스 분포 — 실측치 (문헌 수치와 완전히 일치)

| 클래스 | 장수 | 비율 |
|---|---|---|
| none | 147,431 | 85.24% |
| Edge-Ring | 9,680 | 5.60% |
| Edge-Loc | 5,189 | 3.00% |
| Center | 4,294 | 2.48% |
| Loc | 3,593 | 2.08% |
| Scratch | 1,193 | 0.69% |
| Random | 866 | 0.50% |
| Donut | 555 | 0.32% |
| **Near-full** | **149** | **0.09%** |

**Near-full 149장이 이 프로젝트의 SECOM 센서 130이다.**
5-Fold 교차검증 시 fold당 검증 샘플이 약 30장으로, 3장 차이만으로
Recall이 10% 요동친다. 일부 선행 연구는 이 문제로 Near-full을 실험에서 제외했다.

**대응**: 우선 포함하여 진행하고, 성능이 확보되지 않으면
**그 사실 자체를 분석 결과로 보고**한다. 숨기거나 조용히 제외하지 않는다.

### 4.3 웨이퍼 맵 크기 — 실측 완료, 리사이즈 방식 확정

실측 결과: **고유 크기 632가지**, 가장 흔한 크기(32,29)조차 전체의 13.4%뿐이고
**상위 20개를 합쳐도 57.5%**밖에 안 된다. 압도적으로 지배적인 크기가 없다.

**결정: 전체를 64×64로 리사이즈**한다 (특정 크기 선별 시 42.5%, 약 34만 장을
버려야 해서 비현실적). 리사이즈는 **최근접 이웃(nearest neighbor)** 방식만 쓴다
— 픽셀 값 0/1/2는 밝기가 아니라 범주(다이 없음/정상/불량)이므로, bilinear 등
보간 기반 리사이즈를 쓰면 실존하지 않는 중간값이 생겨 데이터가 오염된다.
구현은 `src/resize_utils.py`의 `resize_nearest()`.

**검증 결과** (`src/02_resize_check.py`, `outputs/resize_before_after.png`,
`outputs/resize_check_summary.txt`):
- 9개 클래스 전부에서 리사이즈 후 픽셀 값이 원본 범위 밖으로 나가지 않음 확인
- 라벨된 172,950장 전체 리사이즈 소요 시간: 1.8초 (장당 0.010ms) — 속도 문제 없음
- **한계 발견**: Scratch 클래스는 원본이 (89,112)처럼 큰 맵에서 다운샘플링될 때
  가늘고 긴 선 패턴의 연속성이 다소 손실되는 경향을 육안으로 확인했다.
  Edge-Loc과의 혼동(7.1절)에 리사이즈 자체가 일부 기여할 가능성이 있다 —
  Phase 4 혼동 행렬에서 실제로 나타나는지 확인 필요.

### 4.4 데이터 구조 (컬럼 정의, 실측 완료 — 2026-08-16)

행 = 웨이퍼 한 장, 전체 811,457행.

**원본 컬럼**

| 컬럼 | 타입 | 의미 | 비고 |
|---|---|---|---|
| `waferMap` | object(2D numpy) | 다이 배치 이미지, 값 0/1/2 | 행마다 크기 다름(632가지) |
| `dieSize` | float | 웨이퍼 전체 다이 수 | `waferMap`에서 값 1 또는 2인 칸 개수와 정확히 일치(실측 확인) |
| `lotName` | object | `"lot1"`~`"lot46293"` | 46,293개 전부 이 형식(정규식 100% 일치) |
| `waferIndex` | float | Lot 내 웨이퍼 순번 | 결측 0개 |
| `trianTestLabel` | object(중첩 배열) | 원 연구자의 Training/Test 구분 | 오타 그대로 저장(4.1절 참조) |
| `failureType` | object(중첩 배열) | 불량 패턴 라벨 | 라벨 없으면 빈 배열 |

**파생 컬럼** (`data_loader.py`의 `load_wm811k()`가 추가): `failureType_clean`, `trainTestLabel_clean`(중첩 배열을 문자열로 정리, 없으면 None), `map_shape`(waferMap.shape 튜플)

**확인된 구조적 관계**
- **라벨 여부 = Training/Test 태그 존재 여부와 정확히 1:1.** 중간 케이스 0건. 단, 라벨된 데이터 안에서 Test 118,595장 vs Training 54,355장으로 Test가 2배 이상 많음 — 이건 원 논문 저자의 구분이므로 **Phase 3 교차검증엔 그대로 쓰지 않고 우리가 직접 나눈다.**
- **Lot당 웨이퍼 수**: 중앙값 24, 75%가 25, 최댓값도 25 — 25장은 반도체 실제 표준 카세트(FOUP) 용량과 일치. Lot 단위가 실제 생산 로트 단위와 맞아떨어짐을 시사.
- `dieSize`는 파생 재계산 없이 그대로 신뢰해서 써도 됨(위 검증 완료).

---

## 5. 기술 스택

| 구분 | 도구 | 비고 |
|---|---|---|
| 딥러닝 | **PyTorch** | 작업자에게 신규. 문법 설명 필요 |
| 해석 | Grad-CAM | SHAP의 이미지 버전 |
| 데이터 | numpy, pandas | SECOM에서 익힘 |
| 평가 | scikit-learn | macro-F1, 혼동 행렬 |
| 시각화 | matplotlib, Plotly | |
| 대시보드 | Streamlit | SECOM에서 익힘 |
| 학습 환경 | Google Colab 무료 GPU | 1~2만 장 규모에 충분 |

**제외**: XGBoost, TreeSHAP (SECOM 전용)

---

## 6. 반드시 지킬 기술적 제약

### 6.1 증강 누수 차단 (최우선)

```python
# 올바른 구조 — 반드시 이 순서를 지킬 것
for train_idx, val_idx in skf.split(X, y):
    X_tr = augment(X[train_idx])   # 학습 fold만 증강
    X_val = X[val_idx]             # 검증 fold는 원본 그대로
    model.fit(X_tr, ...)
    evaluate(model, X_val, ...)

# 잘못된 구조 — 절대 하지 말 것
X_aug = augment(X)                 # 전체를 먼저 증강
for train_idx, val_idx in skf.split(X_aug, y_aug):
    ...                            # 같은 원본의 변형이 양쪽에 들어감
```

회전·뒤집기 증강은 웨이퍼 맵의 라벨을 바꾸지 않으므로 물리적으로 타당하지만,
분할 전에 적용하면 SECOM의 SMOTE 누수와 동일한 문제가 발생한다.

### 6.2 평가 지표

- **주 지표**: macro-F1, balanced accuracy
- **보조**: 클래스별 Precision/Recall, 9×9 혼동 행렬
- **정확도**: 보고는 하되 함정임을 반드시 명시

### 6.3 성능 보고

- 모든 수치는 교차검증 예측 기준
- 학습 데이터 재예측 값은 비교 설명용으로만 사용
- 클래스별 편차를 평균으로 가리지 않는다

---

## 7. 이 프로젝트의 핵심 논점

### 7.1 Edge-Loc vs Scratch — PI 직무와 직결

두 패턴은 **웨이퍼 가장자리 근처의 길쭉한 클러스터**라는 점에서 형태가 유사하다.
그러나 근본 원인이 전혀 다르다.

| 패턴 | 공정 모듈 | 원인 | 조치 |
|---|---|---|---|
| Edge-Loc | 어닐링 / 로드락 | 온도 불균일, 밸브 오염 | **열공정 튜닝** |
| Scratch | 핸들링 | 기계적 접촉 | **이송 장비 점검** |

오분류하면 팹의 시정 조치가 엉뚱한 공정 모듈로 향한다.
**혼동 행렬의 이 칸을 정량화하는 것 자체가 분석 결과**이며,
PI 면접에서 말할 수 있는 이야기다.

### 7.2 공정 원인 매핑 전체

| 패턴 | 공정 모듈 | 물리적 원인 |
|---|---|---|
| Center | 챔버 / 포토리소그래피 | RF 동작 불규칙, 유체 흐름 불균일 **또는** CD(임계치수) 중심-가장자리 불균일 |
| Donut | 세정 | 포토레지스트 잔류물 미제거 |
| Edge-Ring | RTP | 온도 조절 이상, 저온 공정 편차 |
| Edge-Loc | 챔버(가장자리 구조물) | **파티클(포커스링·클램프링 등 가장자리 인접 구조물에서 박리)** 또는 어닐링/로드락 온도·밸브 문제 |
| Loc | 챔버(중심부·상부 구조물) | **파티클(샤워헤드·챔버 돔 등에서 박리해 낙하)** 또는 특정 위치 반복 결함 |
| Scratch | 핸들링 | 기계적 접촉, 이송 장비 |
| Random | 파티클 | 오염, 환경 요인 (Loc/Edge-Loc과 같은 "파티클" 축이지만 다수의 작은 입자가 산발적으로 낙하한 경우) |
| Near-full | 전면 이상 | 공정 전체 실패 |

**주의**: 이 매핑은 문헌 기반의 일반적 대응이다. 실제 팹에서는 장비와 공정에 따라
다를 수 있으므로, 발표 시 "문헌상 보고된 대응 관계"로 명시한다.

**Center 추가 가설 — CD 중심-가장자리 불균일**: 포토리소그래피·식각 공정은
렌즈 수차, 가스 흐름의 방사형 패턴, 온도 분포 등으로 중심과 가장자리의
CD(선폭)가 구조적으로 다르게 나오는 경우가 흔하다. 중심부 CD가 스펙을
벗어나면 그 부분 다이들이 전기적 검사에서 무더기로 탈락해 "Center" 형태의
웨이퍼 맵이 나온다 — 기존 "챔버 RF" 가설과 별개로 똑같이 타당한 문헌상
원인이며, 이 데이터엔 CD 실측값이 없어 둘 중 무엇인지 구분 불가능하다.
Phase 5 Grad-CAM에서 Center의 근거가 유독 약했던 것(0.6배, 히트맵이
중앙에서 흩어짐)도 "하나의 뚜렷한 덩어리"가 아니라 "중심부 전반에 걸친
미세한 CD 이동"이라면 설명이 되는 정황이 있다 — 다만 상관관계 수준의
추정이며 확정 아니다.

**Donut 관련 논의 — FEOL 문제일 가능성?**: PR(포토레지스트)은 스핀(회전)
도포되므로, 도넛 패턴이 가장자리 최외곽은 정상이면서 그보다 안쪽에
고리 모양 불량이 있다면 "코팅 자체보다 더 이전 단계(FEOL)의 문제 아닌가"
하는 가설을 검토했다. 다만 다음 두 가지를 고려하면 확신하기 어렵다:
1. 스핀 코팅은 보통 최외곽 몇 mm를 EBR(Edge Bead Removal)로 별도
   제거한다 — 즉 "가장자리가 멀쩡한 것"은 FEOL이 정상이라서가 아니라
   **EBR이 원래 그 자리를 지우기 때문**일 수 있어, 가장자리 상태만으로
   FEOL 여부를 가르기 어렵다.
2. 오히려 "특정 반경에서만 고리 모양으로 나타나는 불량"은 스핀 코팅
   자체의 유체역학(회전 중 레지스트 두께가 특정 반경에서 물결치듯
   불균일해지는 현상)이나 노광·현상 공정의 반경별 편차로도 흔히
   설명된다 — 즉 오히려 기존 "세정/PR 잔류물" 가설과 더 가까운 결일
   수 있다.
FEOL 쪽 원인(임플란트 각도·도즈의 반경별 편차, 열공정의 반경별 온도
구배 등)도 원리상 배제는 못 하지만, 이 데이터엔 레시피·공정 단계 정보가
전혀 없어 **어느 쪽도 확정할 수 없다** — 문헌·원리 기반의 두 가설이
공존하는 상태로만 기록한다.

### 7.3 나머지 패턴 재분석 — 추가 고려사항 (2026-08-20)

Center·Donut과 같은 방식으로 나머지 6개 패턴도 다시 파고들어, 기존
매핑(7.2절 표)에 없던 대안·보완 가설과 우리 자신의 실측 결과(Grad-CAM
배율, 혼동 행렬, SPC 관리도) 사이의 연결점을 정리한다. **전부 문헌·원리
기반 추정이며, 이 데이터로 확정된 것은 하나도 없다.**

**Edge-Ring (F1 0.968, 최고 성능이지만 Grad-CAM은 0.8배로 통제군 이하)**
- 기존 가설(RTP 온도 편차) 외에, **ESC(정전척) 가장자리 접촉 불량**도
  똑같이 유력한 문헌상 원인이다 — 식각·CVD 공정에서 척의 가장자리
  열전달이 중심부보다 나빠 가장자리 다이만 온도가 달라지는 현상은
  RTP보다 오히려 더 자주 인용되는 Edge-Ring 원인이다.
- **포토 스테퍼의 가장자리 필드 잘림**(사각형 노광 필드가 원형 웨이퍼
  가장자리에 완전히 안 들어가 일부만 노광되는 다이가 생기는 현상)도
  후보다.
- **흥미로운 점**: 분류 성능은 가장 좋은데 Grad-CAM 집중도는 오히려
  통제군보다 낮다(0.8배). "가장자리 근처 밀도가 얼마나 높은가"라는
  전역적 신호만으로도 쉽게 맞힐 수 있어서, 모델이 굳이 고리 모양을
  픽셀 단위로 정확히 짚을 필요가 없었을 가능성 — Donut과 같은 종류의
  "정량 지표가 판단 근거의 질을 다 못 담는" 사례로 추정.
- SPC에서 Edge-Ring이 그룹#54(발생률 22.7%, Z=+70.9)로 가장 크게
  튀었는데, **같은 그룹#54에서 Near-full도 같이 튀었다**(미해결 관찰,
  6절). 만약 실제 사건이라면 "같은 Lot 배치에 심각한 설비 이상이
  있었고, 웨이퍼가 보트/챔버 내 위치에 따라 한쪽은 가장자리만,
  다른 쪽은 전면적으로 영향받았다"는 시나리오가 그림이 맞는다 — 다만
  검증 불가능한 가설이다.

**Edge-Loc과 Loc (묶어서 재검토, 2026-08-20 업데이트)** — 사용자 의견 반영:
Loc·Edge-Loc은 둘 다 "듬성듬성한 점"이 아니라 **뭉친(contiguous) 국소
덩어리**라는 공통점이 있다. 이건 챔버 내벽·전극·샤워헤드 등에 쌓인
반응 부산물이 **박리(flake)돼 웨이퍼 위로 떨어지는 현상(장비성 파티클)**과
정확히 들어맞는다 — 떨어진 한 조각이 그 자리를 중심으로 이후 공정을
막아 하나의 뭉친 덩어리를 만든다. 기존에 각각 따로 적어뒀던
"어닐링/로드락"(Edge-Loc), "국소 설비"(Loc)보다 **"챔버 내부 어디서
파티클이 떨어졌는가" 하나의 축으로 통합 설명**하는 게 더 타당하다:
- 웨이퍼 **가장자리 인접 구조물**(포커스링·클램프링·엣지 실드 — 실제로
  식각/플라즈마 챔버에서 웨이퍼 바로 주변에 있음)에서 박리 → **Edge-Loc**
- 웨이퍼 **위쪽/중심부 구조물**(샤워헤드, 챔버 돔)에서 박리 → 위치 제약
  없는 **Loc**

이 관점은 **Random과도 하나의 축으로 연결**된다 — 큰 조각 하나가 한 번
떨어지면 Loc/Edge-Loc(뭉친 덩어리), 작은 파티클이 여러 개 계속 떨어지면
Random(산발적 분포)이라는 "파티클 사건의 규모·빈도" 차이로 셋을 통합
설명할 수 있다. 기존에 적었던 어닐링/로드락 온도 문제나 레티클(포토마스크)
반복 결함도 여전히 배제되진 않는 대안이지만(레티클 결함은 "필드마다
일정한 격자 간격으로 반복"되는지 확인하면 파티클 가설과 구분 가능,
이 프로젝트 범위에서는 확인 안 함), **파티클 낙하가 더 표준적이고
메커니즘이 구체적인 1순위 가설**로 갱신한다.

이 관점에서 우리 실측 결과를 다시 보면:
- **Edge-Loc** (F1 0.844, Grad-CAM 1.0배): 혼동 행렬 기준 Loc으로
  4.9%(98/2,000), Scratch로는 1.75%(35/2,000)만 샌다 — 원래 걱정했던
  Scratch보다 오히려 Loc과 더 헷갈리는데, 이는 두 패턴이 "파티클
  낙하"라는 같은 근본 메커니즘을 공유한다는 이번 가설과 결이 맞는다
  (물리적으로도 비슷한 원인이니 모양도 비슷하게 나오는 것이 자연스럽다).
- **Loc** (F1 0.769, Grad-CAM 1.4배로 양호): Loc이 원 라벨링 체계에서
  "다른 8개 범주에 안 들어가는 나머지"에 가까운 잔여(residual)
  카테고리라는 점은 여전히 유효한 한계다 — 파티클 낙하가 유력한
  1순위 원인이어도, 그 밑에 다른 물리적 원인이 더 섞여 있을 가능성은
  남는다. Grad-CAM 배율(1.4배)이 분류 성능(F1 0.769)보다 상대적으로
  좋다는 건 **모델이 맞혔을 때는 진짜 근거를 보고 맞힌다**는 뜻 —
  문제는 "어디를 보는가"가 아니라 Scratch·Edge-Loc과 모양이 겹칠 때
  구분을 못한다는 변별력 쪽에 있다. 혼동 행렬 기준 Loc은 Scratch로
  6.8%(136/2,000), Edge-Loc으로 6.35%(127/2,000) 새고, 반대로
  Scratch의 19.6%(234/1,193)가 Loc으로 새는 것과 짝을 이룬다
  (7.1절 핵심 논점의 실제 크기).

**Scratch (F1 0.697, 유일한 미달성이지만 Grad-CAM은 1.6배로 전 클래스 중 최고)**
- 기존 가설(이송 장비 접촉) 외에, **스크래치의 기하학적 모양으로
  하위 원인을 구분**할 수 있다는 게 이번에 정리하며 든 생각이다:
  웨이퍼를 가로지르는 긴 대각선/직선 형태는 로봇 암 이송 중 긁힘에
  가깝고, 가장자리에만 국한된 짧은 자국은 카세트/FOUP 슬롯 삽입 시
  접촉에 가깝다. 리포트 05의 Grad-CAM 예시 이미지들은 전자(웨이퍼를
  가로지르는 긴 대각선)에 가까웠다 — 이송 장비 가설과 정합적.
  **전기 검사용 프로브 접촉 자국**(프로브 카드 정렬 불량으로 탐침이
  표면을 끄는 경우)도 선형 자국을 남기는 별도 후보다.
- Grad-CAM 배율이 전 클래스 중 가장 높다(1.6배)는 건 "모델이 선의
  위치를 정확히 안다"는 뜻인데도 F1이 가장 낮다 — Phase 1에서 이미
  찾은 **리사이즈 단계의 정보 손실**이 진짜 병목이라는 기존 결론과
  다시 한번 정합적으로 맞아떨어진다(위치는 알지만 세밀한 형태 정보
  자체가 입력 단계에서 이미 뭉개졌다는 뜻).

**Random (F1 0.922, Grad-CAM 1.3배로 양호)**
- 위 Loc/Edge-Loc 재검토에서 이미 다뤘듯, Random도 "파티클" 축 위에서
  Loc/Edge-Loc과 한 계열로 묶인다 — 다만 이쪽은 **작은 파티클이 여러
  개 산발적으로 낙하한 경우**에 해당한다는 점만 다르다.
- 이와 별개로, **공정 마진(공정 윈도우) 부족**도 구분해서
  고려할 만하다 — 진짜 이물 파티클이 원인이면 오염 관리(챔버 세정,
  필터 점검)가 정답이지만, 특정 스펙 근처에서 공정이 너무 타이트하게
  운영되고 있어서 다이 간 고유 변동(순수 확률적 노이즈)만으로도 무작위로
  스펙을 넘나드는 경우라면 정답은 완전히 다르다(레시피 중심값 재조정,
  스펙 마진 확보). 두 경우 모두 웨이퍼 맵에서는 똑같이 "무작위로 흩어진
  불량"으로 보이므로, 이 데이터(웨이퍼 맵 이미지)만으로는 구분이
  근본적으로 불가능하다 — 실제 팹이라면 다이 단위 파라메트릭 계측값을
  같이 봐야 구분 가능.

**Near-full (F1 0.940, 149장뿐인데도 의외로 최상위권. Grad-CAM 1.4배, 평균 불량다이비율 87.6%로 압도적 1위)**
- 기존 가설(공정 전체 실패)을 좀 더 세분화하면: **설비 결함으로 인한
  미가동**(예: 플라즈마 미점화, 가스 흐름 차단 — 처리 자체가 안 됨),
  **잘못된 레시피 투입**(다른 공정용 레시피가 실수로 적용됨),
  **웨이퍼 방향/식별 오류**(뒤집혀 들어가거나 다른 웨이퍼와 뒤바뀜) 세
  갈래로 나뉘고, 셋 다 "전면 이상"이라는 결과는 같지만 **시정 조치는
  완전히 다르다**(설비 정비 vs 레시피 관리·인터록 점검 vs 취급
  SOP·센서 점검) — 실무에서는 이 구분이 핵심이다.
- Near-full은 다른 8개 패턴과 성격이 다르다는 점도 명시할 가치가 있다.
  나머지는 "공정이 서서히 어긋난 결과"(점진적 drift)에 가깝지만,
  Near-full은 다이의 87.6%가 불량이라는 것 자체가 **점진적 drift가
  아니라 이분법적 사건(공정이 아예 안 됐거나, 완전히 잘못됐거나)**
  이라는 뜻 — 그래서 149장뿐이라는 표본 부족 문제와 별개로, 애초에
  이 패턴을 다른 8개와 "같은 성격의 문제"로 취급하는 것 자체가
  타당한지도 재검토할 여지가 있다.
- SPC에서 Near-full이 Edge-Ring과 그룹#54에서 동시에 튄 것(위 참고)도
  "심각한 단발성 사건이 그 Lot 배치 전체에 영향을 줬다"는 가설과
  연결된다.

**총평**: 이번 재분석에서 가장 중요한 두 가지는 (1) **Loc·Edge-Loc·Random이
"파티클 낙하"라는 하나의 축으로 통합 설명된다**는 점(챔버 내 낙하 위치와
사건 규모·빈도의 차이일 뿐)과, (2) **Loc과 Near-full은 "패턴→모듈" 1:1
매핑 틀 자체가 다른 패턴들만큼 깔끔하게 들어맞지 않는다**는 점이다 —
Loc은 정의상 잔여 카테고리라 파티클 낙하 외에 다른 원인이 더 섞여 있을
가능성이 크고, Near-full은 점진적 drift가 아니라 이분법적 사건이라
성격이 다르다. 나머지(Center/Donut/Edge-Ring/Scratch)는 "그럴듯한 대안
가설이 최소 1개씩 더 있다"는 정도로 정리되며, 이 데이터로 어느 가설이
맞는지 확정할 방법은 없다.

### 7.4 Lot 응집도 — 위 가설의 첫 데이터 측 방증 (Phase 8, 2026-08-22)

7.3절까지의 논의는 **전부 문헌·원리 기반 추정**이었고 데이터로 검증된 게
하나도 없었다. Phase 8에서 처음으로 **이 데이터 안에서 확인 가능한 방증**을
하나 찾았다 — 다만 아래 한계 때문에 여전히 "확정"은 아니다.

**측정한 것**: Lot은 같은 카세트로 **같은 공정 경로를 같이 통과한** 묶음이다
(Phase 1에서 Lot당 웨이퍼 수 중앙값 24, 최댓값 25로 실제 FOUP 표준 용량과
일치함을 확인). 그렇다면 원인의 성격에 따라 Lot 내 응집도가 갈려야 한다:
- **공정 조건**이 원인 → Lot 전체가 같은 조건을 겪음 → **강하게 뭉쳐야 함**
- **웨이퍼 개별 확률 사건**이 원인 → 한 장만 영향 → **거의 안 뭉쳐야 함**

**통제군 설계(2.2절 ⑥ 적용)**: 그냥 재면 무조건 높게 나온다 — 라벨의 85%가
none이라 Lot과 무관해도 0.85²=72%가 나온다. 그래서 **라벨을 전체에서
무작위로 섞어 Lot 구조를 파괴한 뒤 같은 계산**을 한 값을 통제군으로 뒀다
(20회 평균).

**결과(D조건 = Test 라벨 + none 제외, 가장 깨끗)**: 전체 불량 일치율
65.52% vs 통제군 22.41% = **2.92배(Z=+115.9)**. 패턴별로는:

| 성격 | 패턴 | 배율 | 절대 일치율 |
|---|---|---|---|
| 공정 조건 | Edge-Ring / Center | 8.35x / 7.70x | 78.55% / 44.77% |
| **개별 사건** | **Edge-Loc / Loc / Scratch** | **1.84x / 1.82x / 2.07x** | 39.43% / 23.94% / 7.55% |
| (설명 안 맞음) | Random | 22.25x | 35.41% |
| 표본 부족 | Donut / Near-full | 9.92x / 14.71x | 11.47% / 6.56% |

**핵심**: Edge-Loc·Loc이 **8개 패턴 중 응집도 최하위(1.8배)**다. 파티클 낙하는
웨이퍼 한 장 단위의 확률 사건이지 Lot 전체 조건이 아니므로 뭉치면 안 되는데,
실제로 안 뭉친다 — 7.3절의 파티클 가설과 정합적이다. Scratch(핸들링, 2.07x)가
같은 대역인 것도 같은 논리. 반대로 Edge-Ring·Center는 RTP 온도 프로파일·CD
불균일 같은 **Lot 전체에 걸리는 조건**이라는 가설과 맞게 강하게 뭉친다.

**반드시 붙일 한계**:
1. **인과가 아니다.** 설비ID·레시피·타임스탬프가 없어 "공정 조건이 원인"을
   증명하지 못한다. "같은 Lot이면 패턴이 비슷하다"는 통계적 연관일 뿐이다.
2. **라벨링 절차 자체가 교란일 수 있다.** 원 연구자가 Lot 단위로 몰아 보며
   라벨링했다면 일치율이 인위적으로 오른다. Lot당 라벨 수 중앙값이 23/25장
   이라 "Lot 통째 라벨링"에 가까운 정황이 실제로 있고, **배제할 방법이 없다.**
3. **Random(22.25x)은 위 이분법에 안 맞는다.** 파티클 축에 넣었는데 강하게
   뭉친다. 지속적 오염(필터 열화·클린룸 환경)이면 Lot 전체에 걸리니 설명은
   되지만 이 데이터로 확인 불가 — **가설과 안 맞는 관찰로 그대로 기록한다.**
4. **Donut(436쌍)·Near-full(259쌍)은 표본이 작다.**
5. **배율과 절대값의 순위가 다르다.** 배율은 희귀 패턴일수록 기계적으로
   커진다(Random이 배율 1위인 이유). "얼마나 뭉치는가"는 절대값,
   "우연이 아닌가"는 배율로 봐야 한다 — SECOM의 "측정 공간 확인"(2.2절 ⑤)과
   같은 종류의 함정이다.
6. **Training 라벨 오염이 여기서도 재발했다** — 9.1절 Phase 8 항목 참조.

---

## 8. 작업 시 주의사항

### 8.1 코드 제공 방식

- **완결된 실행 가능 파일**로 제공한다. 조각을 여러 개 던지지 않는다.
- 파일이 길면 단계를 나누되, 각 단계가 독립적으로 실행되고 검증 가능해야 한다.
- 실행 전에 **무엇이 출력될 것으로 예상되는지** 알려준다.

### 8.2 검증 우선

작업자는 SECOM에서 여러 차례 오류를 직접 발견한 경험이 있다.
(대시보드가 학습 데이터를 예측하던 문제, 교호작용 측정 공간 오류 등)

따라서 코드를 제공하기 전에 **가능하면 직접 실행해 검증**한다.
검증 없이 제공할 경우 그 사실을 명시한다.

### 8.3 정직한 보고

- 성능이 나쁘게 나오면 그대로 보고하고 원인을 분석한다
- "잘 나왔다"고 포장하지 않는다
- 불확실한 것은 불확실하다고 말한다
- 외부 연구와 비교할 때는 **프로토콜 차이**를 반드시 확인한다
  (SECOM에서 평가 방식이 다른 논문과 F1을 직접 비교했다가 정정한 사례가 있다)

### 8.4 하지 말 것

- 검사 물량 절감 프레임 사용 — 반도체 양산은 전수검사가 전제다.
  작업자가 직접 이 오류를 지적해 SECOM에서 폐기한 프레임이다.
- 상관관계를 인과관계로 서술
- 클래스별 편차를 평균으로 가리기
- 데이터 증강을 분할 전에 적용

---

## 9. 현재 상태와 다음 작업

### 9.1 완료

- SECOM 프로젝트 완성 및 배포 (별도 저장소)
- 데이터셋 조사 및 클래스 분포 확인
- 실행 계획 수립 (`PLAN.md`)
- **Phase 1 완료**: `LSWMD.pkl` 로드 성공(호환성 3종 해결), 클래스 분포·크기 분포·Lot 정보·픽셀 값 전부 실측 확인 (4절 참조)
- **리사이즈 방식 확정 및 검증**: 64×64 최근접 이웃 리사이즈로 결정, 실제 데이터로 검증 완료 (4.3절 참조)
  - 산출물: `src/data_loader.py`, `src/resize_utils.py`, `src/02_resize_check.py`, `outputs/resize_before_after.png`
- **Phase 2 완료**: 시각화 6종(패턴 그리드/크기 분포/클래스 불균형/클래스별 평균 불량률 맵/불량률-dieSize 산점도/Lot 순서별 발생 비율) 생성 및 HTML 리포트로 통합
  - 산출물: `src/03_visualization.py`, `outputs/02_visualization_report.html`, `outputs/viz_A~F_*.png`
  - **새로 발견한 사실 2가지** (둘 다 리포트 D·F 섹션에 정리됨):
    1. "none"(정상 라벨) 웨이퍼도 전체 평균 불량률 10.55%인데 정중앙 부근만 25.61%로 두 배 이상 높음. Loc도 동일 경향(14.96%→28.18%), Edge-Ring은 차이 없음(15.19%→15.22%). Center 패턴과 무관하게 중앙 기준 다이가 관행적으로 낮게 잡힐 가능성 — 상관관계 관찰이며 확정된 인과관계 아님.
    2. Lot 순서 구간별 불량 비율 그래프에서 초반 구간(라벨 55~500장)의 급격한 스파이크는 표본이 작아 생기는 통계적 잡음일 가능성이 큼 — 후반 구간(1만~2만 장 이상)은 훨씬 평평함. Phase 6 SPC 관리도에서도 구간별 표본 크기를 반드시 같이 표시해야 함.
- **데이터 개요 리포트 작성**: 컬럼 구조·라벨 9종·"데이터로 알 수 있는 것/없는 것"·공정 데이터 유무를 정리한 HTML 문서
  - 산출물: `src/data_overview_report.py`, `outputs/data_overview_report.html`
  - **명시한 핵심 사실**: 이 데이터셋엔 실제 공정(CD/RF파워/챔버온도/레시피/설비ID/타임스탬프) 데이터가 없다. CLAUDE.md 7.2절의 공정 모듈 매핑은 문헌 기반 도메인 지식이지 이 데이터로 검증된 결론이 아님 — 발표 시 이 구분을 명확히 할 것.
- **Phase 3 완료**: 샘플링(다수 클래스 2,000장 축소 + 소수 클래스 전량, 합계 12,763장) → 64×64 리사이즈 → 저장, 그리고 5-Fold 증강 누수 차단 구조를 실제로 검증
  - 산출물: `src/augment.py`(8배 증강), `src/04_preprocessing.py`(PLAN.md상 `03_preprocessing.py`에서 번호 변경), `data/processed/wm811k_sample.npz`, `outputs/phase3_preprocessing_summary.txt`
  - **검증 결과**: 5개 fold 전부에서 검증셋 크기가 증강 전후 변하지 않음 확인(누수 없음), 학습 fold 소수 클래스는 정확히 8배 증강됨(예: fold 1 - 2,210장→17,680장)
  - **재확인된 한계**: fold당 Near-full 검증 샘플 29~30장 — CLAUDE.md 4.2절에서 예고한 불안정성 그대로 재현됨. Phase 4 결과 해석 시 이 점을 반드시 고려.
  - **기술 메모**: Windows 콘솔 코드페이지(cp949)가 `—` 등 일부 유니코드 문자를 못 받아 `print()`가 죽는 문제 발견 → 모든 스크립트 상단에 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` 추가로 해결
- **Phase 4 완료**: CNN 5-Fold 학습 및 평가. **최종 확정 결과: macro-F1 0.8728, balanced accuracy 0.8779** (`src/05_train_evaluate.py`, RUN_TAG="v2_flatten")
  - **PLAN.md 7절 목표 달성**: 최소 목표(9개 중 7개 이상 F1≥0.80) 정확히 7개 달성, 우수 목표(macro-F1≥0.85)도 달성. Edge-Loc↔Scratch 혼동도 목표(10% 미만) 달성(1.8%, 4.4%)
  - **미달성**: Loc(F1 0.769), Scratch(F1 0.697) — 특히 Scratch는 우수 목표(0.70) 근처에서 아깝게 미달
  - **1차 시도(GAP 구조)는 실패**: macro-F1 0.7691로 최소 목표 미달. 원인 분석 결과 GAP이 위치·모양 정보를 지워, Center/Loc/Scratch/Donut처럼 "위치·모양"이 정체성인 클래스가 특히 나빴음(Loc이 다른 클래스와 뒤섞이는 허브였음 — Center→Loc 24.5%, Scratch→Loc 20.0%)
  - **개선 Step 1(Flatten head) 채택 확정**: GAP 대신 8×8 위치 정보를 그대로 Dense층에 넘기는 구조로 교체. macro-F1 0.7691→0.8728로 대폭 개선. Center→Loc 혼동 24.5%→2.25%로 급감
  - **개선 Step 2(해상도 128×128) 폐기**: Scratch 개선을 노렸으나 오히려 전 클래스 악화(macro-F1 -0.052, Scratch -0.166). 원인 추정: 입력 해상도만 키우고 Conv 층수를 안 늘려 상대적 수용 영역이 좁아짐 + 마지막 AdaptiveMaxPool 압축 단계가 새로운 정보 손실을 만듦
  - **개선 Step 3(선형성 보조 특징) 폐기**: `src/shape_features.py`로 불량 덩어리의 PCA 기반 선형성(연결요소 중 최대 덩어리만 사용)을 계산해 보조 입력으로 추가. Scratch는 개선(+0.031)됐으나 Loc을 포함한 대부분의 다른 클래스가 소폭 악화되어 전체 macro-F1은 순손실(-0.016)
  - **결론**: Scratch(0.697)는 Phase 1에서 발견한 리사이즈 단계의 선 연속성 손실이 근본 원인으로, 모델 구조 개선(Flatten)이나 보조 특징으로는 해결되지 않는 데이터 단의 한계로 판단하고 여기서 개선 시도를 종료함
  - 산출물: `src/model.py`(GAP/Flatten head 선택 가능), `src/05_train_evaluate.py`, `src/05a~05f_*.py`(비교 실험들), `src/shape_features.py`, `outputs/phase4_*.txt/json/npz`, `artifacts/model_v2_flatten_fold0~4.pt`
- **Phase 5 완료**: Grad-CAM으로 fold 0 모델의 예측 근거를 시각화하고, 통제군(히트맵이 무작위였을 때의 기대값) 대비 정량 평가
  - 산출물: `src/gradcam.py`(hook 기반 Grad-CAM 구현), `src/06_gradcam.py`, `outputs/phase5_gradcam_grid.png`, `outputs/report05_gradcam.html`(리포트 05)
  - **정량 결과(통제군 대비 배율)**: Scratch 1.6배·Loc 1.4배·Near-full 1.4배·Random 1.3배는 뚜렷이 높음. Edge-Loc·none은 1.0~1.1배로 통제군과 비슷. **Donut 0.9배·Edge-Ring 0.8배·Center 0.6배는 오히려 통제군보다 낮음.** 전체 평균은 1.0배로 사실상 통제군 수준.
  - **정량 지표와 육안 확인이 어긋난 사례**: Donut은 정량 지표는 낮게(0.9배) 나왔지만 육안으로는 히트맵이 도넛 고리 모양을 뚜렷이 따라감. Loc은 덩어리 자체가 아니라 그 **윤곽선(경계)**을 고리 모양으로 감싸는 패턴이 관찰됨 — "칸이 정확히 겹쳐야 인정"하는 지표 설계가 윤곽선 기반의 정당한 판단 근거를 과소평가하는 것으로 추정. 숫자만으로 판단하지 않고 반드시 그림과 같이 봐야 한다는 사례로 기록.
  - **정직하게 명시할 한계**: Center(0.6배)는 육안으로도 히트맵이 중앙 덩어리에서 벗어나 흩어지는 경향이 실제로 보임 — 이 클래스는 정량·정성 둘 다 근거가 약하다고 판단됨.
- **Phase 6 완료**: SECOM `main2.py`의 p-관리도 로직(p_bar, UCL=p_bar+3√(p_bar(1-p_bar)/n), scipy binomtest)을 그대로 가져와 Lot 단위로 재구성
  - 산출물: `src/07_spc_control_chart.py`, `outputs/phase6_spc_control_chart.png`, `outputs/phase6_group_sizes.png`, `outputs/phase6_yield_contribution.csv`, `outputs/report06_spc.html`(리포트 06)
  - **중요 발견(즉시 반영함)**: 전체 Lot 46,293개 중 라벨 있는 Lot은 10,762개(23.2%)뿐이라 Lot 단위 그대로는 관리도가 불가능 — "라벨 1,000장 도달할 때까지 연속 Lot을 묶는" 방식으로 대체(Phase 2의 "Lot 번호 범위 균등분할" 대신, 표본 크기를 고르게 맞춰 UCL 폭을 공정 비교 가능하게 함).
  - **더 중요한 발견**: 처음 관리도를 그렸을 때 거의 모든 패턴이 Lot 순서 초반 구간에서 동시에 극단적으로 치솟았는데, 확인해보니 **그 구간 웨이퍼의 100%가 원 연구자의 "Training" 라벨**이었다 — 실제 생산 이상이 아니라 원 연구자가 균형 잡힌 학습셋을 만들려고 다양한 불량 유형을 골고루 뽑은 세트가 Lot 순서상 뭉쳐 있어 생긴 착시. **"Test" 라벨(118,595장)만 남기고 재계산**해서 반영함 — Training/Test 태그를 "재사용하지 않는다"는 기존 원칙이 Lot 순서 분석에서 이렇게 극적으로 드러날 줄은 예상 못 했던 사례로 기록.
  - **수율 손실 기여도(발생률×평균 불량다이비율) 순위**: Edge-Loc(1위) > Loc(2위) > Edge-Ring(3위) > Random > Center > Near-full > Scratch > Donut. Near-full은 발생률은 최하위(0.08%)지만 다이당 불량 비율이 87.6%로 압도적이라 발생 시 피해가 매우 큼.
  - **후속 확인 필요(미해결)**: Edge-Ring·Near-full이 그룹#54에서, Center·Random이 그룹#76에서 동시에 관리한계를 초과 — 복합적 공정 이상인지 또 다른 데이터 구조상 우연인지 이 프로젝트 범위에서는 미확정.

- **Phase 7 완료**: Streamlit 5화면 대시보드 구축 및 GitHub 배포
  - 산출물: `app.py`, `src/08_precompute_dashboard.py`, `artifacts/dashboard_bundle.joblib`, `requirements.txt`, `.gitignore`, `.claude/launch.json`
  - 저장소: `https://github.com/LemonLime-ABC/wafer-map-dashboard` (Streamlit Community Cloud 배포용)
  - **구조**: SECOM `precomputed.joblib` 방식을 그대로 차용 — 대시보드는 무거운 계산을 직접 하지 않고 사전 계산된 joblib 하나만 읽는다. 원본 2GB `LSWMD.pkl`은 `.gitignore`로 제외(배포된 앱은 이 파일을 안 씀).
  - **버그 1건(사용자가 발견)**: 웨이퍼 맵 컬러스케일의 컬러스톱이 정확히 0.5에 중복돼 **정상 다이(1)와 불량 다이(2)가 같은 빨강으로 렌더링**되고 있었다. 중복 없는 평탄 색 밴드로 재설계했고, 수정 후 렌더된 SVG의 base64 PNG를 JS canvas로 픽셀 샘플링해 RGB 3색이 실제로 구분되는지 직접 확인했다. **"고쳤다"는 주장 대신 픽셀 단위로 검증한 사례.**
- **Phase 8 완료(2026-08-22)**: "같은 Lot의 웨이퍼는 같은 불량 양상을 보이는가" 실증 + 대시보드 확률 분포 표시
  - 산출물: `src/09_lot_homogeneity.py`, `outputs/phase8_lot_homogeneity.txt`, `outputs/phase8_lot_pattern_cohesion{,_test}.csv`
  - **결과 요약**: Lot 내 불량 패턴 일치율 65.52% vs 통제군 22.41% = **2.92배(Z=+115.9)**. 패턴별로 응집도가 크게 갈리며, 그 갈림이 7.3절 파티클 가설과 정합적이다 — **상세와 한계 6가지는 7.4절 참조.**
  - **Training 라벨 오염 재발(2번째)**: Training 포함 시 92.73%, Test만 쓰면 65.52%로 27%p 차이. Phase 6 SPC에서 겪은 것과 **정확히 같은 함정**이 다시 걸렸다. → **이 데이터셋에서 Lot 관련 분석을 할 때는 `trainTestLabel_clean == "Test"` 필터가 사실상 필수**라고 봐야 한다. 앞으로 Lot 단위 집계를 새로 만들 때 이 필터를 먼저 넣을 것.
  - **성능 최적화 메모**: 쌍(pair)을 하나하나 세면 2,700만 번 반복이라 수 분이 걸린다. 조합 공식(같은 라벨 쌍 수 = ΣC(n_k,2))으로 바꿔 `np.bincount` 한 번에 계산하도록 하여 수 초로 단축. **Phase 4에서 3시간짜리 실험을 방치했던 전례(9.1절 Phase 4 항목)를 반복하지 않으려 실행 전에 런타임을 먼저 추산한 결과.**
  - **대시보드 개선**: 화면 1에 **9개 클래스 전체 예측 확률 막대그래프** 추가(빨강=예측/초록=실제/회색=나머지). 기존엔 최댓값 하나만 저장하고 있어 "0.99로 확신"과 "0.35 vs 0.33으로 간신히 이김"이 구분되지 않았다.
    - **정합성 검증**: 번들 정렬이 어긋나면 엉뚱한 웨이퍼의 확률이 조용히 표시되므로 `argmax(probs_all)==pred_label`(2,553장 전부 일치), `max==pred_prob`, 행 합계=1.0을 직접 확인함.
    - **부수 발견**: 1-2등 확률 격차가 **20%p 미만인 웨이퍼 81장 중 59.3%가 오분류** — 격차 20%p 이상 웨이퍼의 오분류율 10.0%의 약 6배. 즉 확률 격차가 "이 예측을 사람이 다시 봐야 하는가"를 실제로 가려낸다. 앱에 **하드코딩하지 않고 매번 계산**하도록 넣음(모델이 바뀌면 조용히 거짓말이 되지 않도록).
    - **프레임 주의**: 이건 8.4절의 "검사 물량 절감" 프레임이 아니다. 전수검사를 전제로 하되 **사람이 재확인할 우선순위를 매기는 것**이라는 점을 발표 시 구분할 것.

- **Phase 9 완료(2026-09-13)**: 대시보드 화면 6 "새 웨이퍼 즉석 판정" — 사용자가 올린 웨이퍼 맵을 그 자리에서 판정(64×64 리사이즈 → CNN → Grad-CAM)
  - 산출물: `src/live_inference.py`(파일 해석·입력 검사·예측), `src/13_unlabeled_samples.py`(미라벨 풀 1,000장·입력 검사 기준값·시연 파일), `src/14_heldout_eval.py`, `artifacts/unlabeled_samples.joblib`, `artifacts/heldout_eval.joblib`, `outputs/phase9_heldout_eval.txt`, `samples/`(시연용 CSV — 전부 라벨 없는 원본)
  - **입력**: CSV/TXT/NPY 여러 장 동시. 값 규칙 2종 — WM-811K(0/1/2), Bin 코드(빈칸=다이 없음, Pass bin=정상, 그 외=불량). 앱 안에서 예시 Lot·미라벨 무작위 추출도 가능(폰 시연용)
  - **검증**: 화면 1 번들 확률과 오차 0 일치(전처리 동일성), CSV 왕복 원본과 동일, Bin 코드 파일이 같은 웨이퍼의 0/1/2 파일과 같은 판정, 잘못된 입력 7종 거절
  - **가장 중요한 발견 — 현장 비율에서 불량 과판정**: 학습 샘플을 픽셀 단위로 재현해 **라벨은 있지만 5개 모델 누구도 본 적 없는 웨이퍼 34,756장**으로 재측정. 형태 분류는 유지(Edge-Ring 95.9%, Center 95.6%, Edge-Loc 84.0%, Loc 77.8%)되지만 **정상 웨이퍼의 12.8%를 불량으로 판정**. 학습 샘플은 정상이 15.7%인데 실제는 85%라서, 현장 비율 가정 시 **불량 판정의 약 43%가 실제 정상**으로 추정(Scratch 판정 86%, Loc 62%, Edge-Loc 45%). 정상 판정은 99.6%가 실제 정상. 소수 4종은 본 적 없는 웨이퍼가 없어 OOF 혼동 행렬로 대신한 **출처 혼합 추정치**
  - **재확인 기준 교체**: 기존 "1-2등 격차 20%p"는 이 오경보의 16%만 잡음. **"5개 fold 모델 만장일치 아님"은 88%를 잡고 맞힌 불량은 9%만 건드림**(5/5 오분류 2.1%, 4/5 18.5%, 3/5 43%). 만장일치 불량 판정만 남기면 정상 웨이퍼 오경보율 12.8%→1.5%. 화면 6의 '재확인 권장'은 두 검증된 기준만 쓰고, 검증 안 된 입력 경고는 별도 열로 분리
  - **미해결**: ①미라벨 1,000장에선 정상 판정이 49%뿐 — 라벨된 분포(85%)라면 약 75%가 나와야 해서, 미라벨 웨이퍼 분포가 라벨된 것과 다를 가능성(확인 불가) ②5모델 평균 확률로 판정하면 정상 정답률 87.2→91.4%로 오르나 소수 4종에서 측정할 방법이 없어 채택 안 함 ③사전확률 보정(prior correction)은 평가셋에 맞춰 조정하면 누수라 별도 검증셋 설계가 필요
  - **시연 파일 교체(같은 날)**: 처음 분위수로 뽑은 샘플에 **가로·세로 줄무늬 웨이퍼**(행·열 통째 불량)가 섞였는데, 9개 패턴 어디에도 속하지 않는 모양을 모델이 "Random 100%, 5/5 만장일치"로 판정했다 — **닫힌 분류기는 분류 체계 밖 모양을 자신 있게 틀리고, 만장일치 기준으로도 못 거른다**는 한계로 기록. 교체 기준: 데이터 특성(학습 범위 안, 줄무늬 없음, 테두리 제외 최대 덩어리 20다이·불량의 40% 이상)으로 거른 뒤 사람이 그림을 보고 모양이 다른 8장과 Lot `lot42002`(원본 25행·라벨 0행, 라벨된 맵과 픽셀 동일 0장)를 고름. 모델 판정은 고를 때 쓰지 않음
  - **프레임 주의**: 재확인 권장은 검사 물량 절감이 아니라 전수검사 전제에서 사람이 다시 볼 순서를 정하는 것(8.4절)

### 9.2 다음 작업

Phase 1~8이 모두 끝났다. 남은 것은 확정된 작업이 아니라 후보다.
- **미해결로 남은 항목**(새로 착수한다면 여기부터): ①SPC 그룹#54·#76의 패턴 동시 초과가 실제 복합 공정 이상인지(9.1절 Phase 6) ②Random이 파티클 가설과 달리 Lot 내에서 강하게 뭉치는 이유(7.4절 한계 3)
- **SK하이닉스 JD 대조 결과(2026-08-22 조사)**: 이 프로젝트와 가장 부합하는 직무는 **기반기술–DMI**("Pattern의 변화를 정량화하고 불량을 검출", "AI 모델/알고리즘을 활용한 반도체 관련 프로젝트 도전 경험" 우대), 다음이 **소자–FA/TCAD**(1지망 PI와 같은 페이지), 그다음이 **양산기술**("TEST/분석 프로그램 개발 등 프로그래밍 활용 경험" 우대). 참고로 2지망이던 "평가및분석(Data Science)"은 이번 JD에 별도 직무로 존재하지 않고 위 세 곳에 흡수돼 있다.

### 9.3 일정

전체 약 5~6주 예상이었으나 Phase 1~8을 약 1주일(2026-08-16 ~ 08-22)에 완료. 상세는 `PLAN.md` 6절 참조.

---

## 10. 참고 — SECOM 프로젝트 산출물

위치 확인됨: `C:\Users\digim\Desktop\시립대 4학년\2.대외활동\여름방학\ECE IF\1. 반도체_수율_프로젝트\`
(wafer_map_project와 같은 상위 폴더 안에 있음). Phase 6에서 실제로 `main2.py`의
p-관리도 로직(p_bar, ucl, scipy.stats.binomtest)을 그대로 가져와 재사용했다.

이전 프로젝트의 코드가 참고 가능하다면 다음이 재사용 대상이다.

| 파일 | 재사용 대상 |
|---|---|
| `main2.py` | 5-Fold 교차검증 구조, 누수 차단 패턴, p-관리도 계산 |
| `app.py` | Streamlit 대시보드 구조, 5화면 구성, Plotly 이중축 그래프 |

특히 **SPC 관리도 로직**(UCL 산출, Z값, 이항검정)은 Phase 6에서
집계 대상만 바꿔 거의 그대로 사용할 수 있다.

```
SECOM:  배치 100장의 "위험 판정 개수" → p-관리도 → UCL 초과 판정
WM:     Lot 단위 "패턴별 발생 빈도"    → p-관리도 → UCL 초과 판정
```
